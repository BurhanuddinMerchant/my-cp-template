#include<iostream>
#include<vector>
#include<iomanip>
#include<cmath>
#include<algorithm>

using namespace std;

#define ll long long
#define ld long double

#define fr(i,n) for(int i=0;i<n;i++)
#define frp(i,u,n) for(int i=u;i<n;i++)
#define frr(i,n) for(int i=n;i>=0;i--)

#define vi vector<int>
#define vl vector<long long>

#define sort(a) sort(a.begin(),a.end())

#define pb push_back

#define setpre(n) setprecision(n)

#define debug(x) cout<<(#x)<<"="<<x<<"\n"
void solution(){
	//your code goes here
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin>>t;
    while(t--){
	solution();
    }
    return 0;
}